const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); await page.goto('https://www.zivefirmy.cz/instalateri_o619?kraj=pardubicky'); await page.waitForTimeout(2000); const html = await page.evaluate(() => { const p = document.querySelector('.strankovani, .pagination, .paging, .pages, .next'); return p ? p.outerHTML : 'no pagination found'; }); console.log('Pagination:', html); const allAs = await page.evaluate(() => { return Array.from(document.querySelectorAll('a')).map(a => a.outerHTML).filter(h => h.includes('Další')); }); console.log('Next links:', allAs); await browser.close(); })();
